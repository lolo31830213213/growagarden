local UIS=game:GetService("UserInputService") local plr=game.Players.LocalPlayer UIS.InputBegan:Connect(function(i,g) if g then return end if i.KeyCode==Enum.KeyCode.E then local char=plr.Character if not char then return end local hrp=char:FindFirstChild("HumanoidRootPart") if not hrp then return end for _,v in pairs(workspace:GetDescendants()) do if v.Name=="ScrapStuff" then local part=v:IsA("Model") and v:FindFirstChildWhichIsA("BasePart") or v if part and (hrp.Position-part.Position).Magnitude<=10 then v:Destroy() print("Scrap pris") end end end end end)

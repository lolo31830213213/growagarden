-- Create a new screen to hold the GUI
local gui = Instance.new("ScreenGui")
gui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Create a container frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0.3, 0, 0.15, 0)
frame.Position = UDim2.new(0.35, 0, 0.4, 0)
frame.BackgroundColor3 = Color3.fromRGB(46, 46, 46)
frame.Parent = gui

-- Create a title label
local titleLabel = Instance.new("TextLabel")
titleLabel.Text = "Combat"
titleLabel.Size = UDim2.new(1, -20, 0.25, 0)
titleLabel.Position = UDim2.new(0, 10, 0, 0)
titleLabel.BackgroundColor3 = Color3.fromRGB(74, 74, 74)
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.Parent = frame

-- Create an AimBot toggle button
local aimbotToggle = Instance.new("TextButton")
aimbotToggle.Text = "AimBot"
aimbotToggle.Size = UDim2.new(1, -20, 0.35, 0)
aimbotToggle.Position = UDim2.new(0, 10, 0.25, 0)
aimbotToggle.BackgroundColor3 = Color3.fromRGB(74, 74, 74)
aimbotToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
aimbotToggle.Parent = frame

-- Create a No Recoil toggle button
local noRecoilToggle = Instance.new("TextButton")
noRecoilToggle.Text = "No Recoil"
noRecoilToggle.Size = UDim2.new(1, -20, 0.35, 0)
noRecoilToggle.Position = UDim2.new(0, 10, 0.6, 0)
noRecoilToggle.BackgroundColor3 = Color3.fromRGB(74, 74, 74)
noRecoilToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
noRecoilToggle.Parent = frame

-- Function to toggle AimBot
local function toggleAimbot()
    if aimbotEnabled then
        print("AimBot disabled")
        UserInputService.MouseDelta = Vector2.new(0, 0)
    else
        print("AimBot enabled")
    end
    aimbotEnabled = not aimbotEnabled
end

-- Function to toggle No Recoil
local function toggleNoRecoil()
    if noRecoilActive then
        print("No Recoil disabled")
    else
        print("No Recoil enabled")
    end
    noRecoilActive = not noRecoilActive
end

-- Connect the toggle buttons to their respective functions
aimbotToggle.MouseButton1Click:Connect(toggleAimbot)
noRecoilToggle.MouseButton1Click:Connect(toggleNoRecoil)

-- Initialize AimBot and No Recoil states
local aimbotEnabled = false
local noRecoilActive = false

-- Function for AimBot logic
local function aimbot()
    if aimbotEnabled then
        local target, _ = getClosestTarget()
        
        if target and target:FindFirstChild("HumanoidRootPart") then
            local humanoidRootPart = target.HumanoidRootPart
            
            local lookVector = (humanoidRootPart.Position - LocalPlayer.Character.HumanoidRootPart.Position).unit
            local aimAngle = math.atan2(lookVector.Z, lookVector.X)
            
            Mouse.LookVector = CFrame.Angles(0, aimAngle, 0).LookVector
            
            local screenPosition = Mouse.Hit.p
            local currentMouseX, currentMouseY = Mouse.Position.X, Mouse.Position.Y
            
            local deltaX = screenPosition.X - LocalPlayer.Character.HumanoidRootPart.Position.X
            local deltaY = screenPosition.Y - LocalPlayer.Character.HumanoidRootPart.Position.Z
            
            local angleX = math.atan2(deltaY, deltaX)
            
            UserInputService.MouseDelta = Vector2.new((angleX - math.pi / 2) * 50, 0)
        end
end
